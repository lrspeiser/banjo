# Physics tests at the first-person assembly table

Local source `6a5fae92ffc4d5fba2484b299ad0917f13f30c6d`, September 5, 2026; not pushed or merged. Full goal remains active.

Assembly Designs now offers Physics Test. Load Test reads the workspace's assembly-test.json (bounded at 1 MiB), shows energy, duration, separated-area criterion and numerical limits, and Run Test calls the existing isolated assembly API on a background task. Actual execution uses the strict shared parser; preview parsing alone is not acceptance. One assembly test may run at a time, independently of the existing single-object ramp test. Completion is polled in the main loop even outside the table. No cancellation was added; shutdown may wait for the bounded task.

Results require both the saved revision and exact canonical declaration to match. A changed design hides the prior pass and export control. Loading settings clears the result. API exceptions are shown as Test rejected; measured pass/fail comes only from the returned report. Export writes draft_revision and the full report/declaration/specification to assembly-test-result.json. This is a historical snapshot, not an automatically current certificate. Neither results nor loaded test settings are persisted in the world save.

Native oak verification exercised Physics Test, Load Test, Run Test and Export. Revision 1 passed with 100% separation, signed energy residual -4.4650925179468e-9 J, and the exported declaration/revision matched the saved design exactly. Importing a joint-ID revision 2 then returning to testing replaced the pass with the explicit stale-result message. The player stayed at level 1 with 100 stamina and empty inventory. The normal starter simulation continues while its table is open; this does not claim the live simulation clock freezes. The isolated job itself receives only copied declaration/settings and cannot change live resources.

The Windows MSVC 19.44 Release starter target builds and all 21 CTest suites pass in 17.89 seconds. Existing glass/oak/iron assembly, stock, persistence and rejection tests remain covered; native test controls this turn were exercised for oak only. Native low-input failure and budget exhaustion were previously checked in the workshop, not newly in this table. No physics law, criterion or tolerance changed. The starter remains open at the test table; the workshop remains available separately.

Next: real LLM review at this table using the starter's inventory context, followed by live assembly creation with authoritative contact ownership and material/energy transactions. Passing this isolated separation criterion does not establish joint usefulness, realistic material behavior or physical cutting. All remaining gates and all 40 scorecard rows are preserved.
